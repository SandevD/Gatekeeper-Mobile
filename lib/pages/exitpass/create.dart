import 'package:flutter/material.dart';

class CreateExitPass extends StatelessWidget {
  const CreateExitPass({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      debugShowCheckedModeBanner: false,
    );
  }
}
